package net.yakclient.websocket.messaging.base

/**
 * Represents a packet(deserialized object) of information that
 * can be serialized into a SocketMessage.
 *
 * @see SocketMessage
 */
interface SocketPacket